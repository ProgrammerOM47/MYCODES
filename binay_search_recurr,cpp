#include<iostream>
using namespace std;
bool search_recur(int arr[],int s,int e,int key){
    

    if(n==0){
        return;
    }
    int m=s+e/2;
    if(arr[m]>key){
        e=m-1;
    }else if(arr[m]<key){
        s=m+1;
    }
    if(arr[m]==key or search_recur(arr,s,e,key)){
        return m;
    }
    

    if(arr[i] )
}
int main(){
    int arr[1000];
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    int key;
    cin>>key;

    if(search_recur(arr,0,n-1,key)){
        cout<<"found"
    }
    return 0;
}